<%= form_for(@heading, :url => {:controller => :headings, :action => :update}, :remote => true) do |f| %>
  <% if @heading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heading.errors.count, "error") %> prohibited this heading from being saved:</h2>

      <ul>
      <% @heading.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="edit_heading">
    <%= f.text_field :title %>
  </div>
  
  <%= f.hidden_field :user_id %>
<% end %>